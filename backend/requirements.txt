# Web framework
fastapi==0.111.1
uvicorn[standard]==0.30.6
python-multipart==0.0.9
websockets==12.0

# Task queue
celery[redis]==5.4.0
redis==5.0.8
flower==2.0.1

# Database
sqlalchemy==2.0.31
alembic==1.13.2

# Configuration — sin pin: tabled-pdf (dep de marker-pdf) requiere pydantic>=2.9.2
pydantic-settings
pydantic

# PDF processing
PyMuPDF==1.24.9
pypdf==4.3.1

# Image processing — sin pin: evitar conflictos con scikit-image/scipy
Pillow
opencv-python-headless
numpy
scikit-image
imutils==0.5.4

# OCR — Marker/Surya (motor principal, PyTorch ROCm compatible)
# torch/torchvision NO están aquí: se instalan desde el wheel ROCm en el Dockerfile
# surya-ocr: sin pin, marker-pdf lo declara como dependencia
marker-pdf==0.3.10

# OCR — TrOCR (manuscritos, HuggingFace Transformers)
# Sin pins: marker-pdf declara sus propios rangos
transformers
huggingface-hub
accelerate
safetensors
sentencepiece
tokenizers

# MinerU (tablas complejas, fallback GPU)
# 1.3.x eliminó la dependencia de detectron2/layoutlmv3 y es compatible con torch>=2.2
magic-pdf==1.3.12
ultralytics
doclayout-yolo
pycocotools

# Ollama client (corrección LLM)
ollama==0.3.3

# HTTP client
httpx==0.27.2
aiofiles==24.1.0
aiohttp==3.10.5

# File monitoring
watchdog==5.0.0

# OCR fallback CPU
pytesseract

# Requerido por magic-pdf 1.3.12
openai
rapid-table==1.0.5
omegaconf
dill

# Utilities
loguru==0.7.2
python-magic==0.4.27
python-slugify==8.0.4
tqdm>=4.67.1
tenacity==9.0.0
